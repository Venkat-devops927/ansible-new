- name: command vs shell
  hosts: local
  connection: local
  tasks:
  - name: excute the command
    ansible.builtin.shell: ls -ltr > /tmp/output.txt

  - name: excute the comand
    ansible.builtin.command: ls -ltr