- name: filter the values
  hosts: local
  connection: local
  tasks:
  - name: conveert to map list
    vars:
      course:
        name: "devops with aws"
        duration: 120hrs
        trainer: shiva
    ansible.builtin.debug:
      msg: "{{ course | dict2items }}"
